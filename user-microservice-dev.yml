user-microservice:
  property: user profile dev
  
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_microservice
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true